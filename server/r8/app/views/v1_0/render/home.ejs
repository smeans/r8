<% const body = loginSession.valid ? include('../widgets/dashboard') : include('../widgets/login') %>
<%- loginSession.valid ? include('../page', {title: "Dashboard", body: body}) : include('../landingpage', {title: "Login", body: body}) %>
