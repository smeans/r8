const wsMap = new Map();

function notifySession(sessionId, message) {
    const ws = wsMap.get(sessionId);

    if (!ws) {
        console.warn('notifySession: socket not found', sessionId);

        return;
    }

    ws.send(JSON.stringify(message));
}

module.exports = {
    wsMap,
    notifySession
}
